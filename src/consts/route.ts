const USERS: string = "/users";
const USER_CREATE: string = `${USERS}/create`;
const USER_UPDATE: string = `${USERS}/update`;

const WIKI: string = "/wiki";

export const PATH = { USERS, USER_CREATE, USER_UPDATE, WIKI };
